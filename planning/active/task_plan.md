# Task: Resolve inferred_format from the camera named in a frame's PAT-B file (#50)

## Problem

A digital frame with no `camera_calibration_url` cannot be sized from its GSD, because
pixel count spreads 32–83% at a given focal length. `fly_footprint()` records
`footprint_basis = "inferred_format"` and produces **no footprint at all** unless a DEM
is supplied — documented behaviour, and correct given what it knows.

But the sensor identity is often published elsewhere, and `fly` already ships the
dimensions. The centroid layer carries `patb_georef_url` for frames with a
photogrammetric solution, and those files name the camera.

Outcome: a frame with no calibration URL but with a PAT-B file is sized on the ordinary
`px_cross x ground_sample_distance` route — **no DEM required** — and `width_source`
records that the format came from a camera the province named rather than an inference.

## Decisions taken at the plan gate

1. **A new exported `fly_camera_patb()`** fetches and parses; `fly_footprint()` never
   touches the network and consumes a `camera_serial` column if one is present.
2. **`footprint_basis` stays the media value**, as for a calibration-URL row. Provenance
   goes in `width_source = "patb_serial=<serial>"`.
3. **Fix the mislabelled camera row and the generator**, plus the invariant test that
   makes name-based resolution safe.

## Phase 1: Measure the schemas and the population

- [ ] Query the catalogue for digital frames with no `camera_calibration_url` and a
      `patb_georef_url`, across years; sample enough archives to enumerate every schema
      variant, not just the two measured while planning
- [ ] Record per schema: file extension, key column, identity columns, coverage —
      into `findings.md`
- [ ] Confirm the `.ori` archives carry no camera identity in any variant
- [ ] Edit the issue body: correct the `20814295` claim, restate the schemas as measured

## Phase 2: Fix the camera label and pin the invariant

- [ ] `camera_name()` in `data-raw/make_camera_formats.R`: derive the label from
      `report_serial`'s family where a serial is present (`UC-SX-` → UltraCam X,
      `UC-SXp-` → UltraCam Xp), falling back to the text scan; QA check that the two agree
- [ ] Patch `inst/extdata/camera_formats.csv` row `70912643_2015` → `UltraCam X`
- [ ] `tests/testthat/test-camera_formats.R`: every distinct `camera` label maps to
      exactly one `(px_cross, px_along)`, with the premise asserted so it cannot pass
      vacuously
- [ ] Restore the wrong label and confirm the test fails

## Phase 3: Serial resolution inside `fly_camera_format()`

- [ ] Build a serial index from `camera_formats.csv`: digits from `key` and from
      `report_serial`, both pointing at the same rows
- [ ] Resolve a `camera_serial` column through that index —
      `width_source = "patb_serial=<serial>"`, `resolved = TRUE`, `inferred = FALSE`.
      Consulted after `camera_calibration_url`, before the focal-length fallback
- [ ] Resolve a `camera_name` column through the label, only where the label maps to one
      format. Serial wins where both are present
- [ ] Refuse rather than guess: an identity resolving to rows that disagree on
      `(px_cross, px_along)` yields no format and
      `width_source = "ambiguous_serial:<serial>"`
- [ ] Tests: resolves by serial, resolves by name, ambiguous, unknown serial, serial and
      calibration URL both present (URL wins), serial on a film frame (ignored)

## Phase 4: `fly_camera_patb()`

- [ ] New `R/fly_camera_patb.R` — downloads missing archives through
      `fly_fetch(type = "georef")`, unzips, dispatches on schema by column names present,
      joins per frame, parses once per URL
- [ ] Join key built from the file's own keys; frame-number width derived, not assumed
- [ ] An archive with only `.ori`, or a schema with no identity column, yields `NA` with
      a message naming the file
- [ ] Bundle a trimmed real PAT-B fixture in `inst/testdata/`, generated by
      `data-raw/make_testdata.R`, so the suite is offline; `skip_on_ci()` live canary
- [ ] Runnable `@examples` on the bundled fixture

## Phase 5: Documentation and release

- [ ] `inst/notes/camera-formats.md`: the PAT-B route, the measured schemas, why the
      serial rather than the key is the join, why the `22814295` gap is not a typo
- [ ] `fly_footprint()` roxygen: how to obtain the camera column
- [ ] `NEWS.md` + `DESCRIPTION` bump to 0.10.0, as the final commit of the branch
- [ ] File the follow-up issue for the DEM film blow-up the issue's own run surfaced
- [ ] `/planning-archive`, then `/gh-pr-push`

## Validation

- [ ] Tests pass
- [ ] `/code-check` clean on each commit
- [ ] `lintr::lint_package()` no new lints against the `HEAD` baseline
- [ ] `devtools::document()` output read; `pkgdown::check_pkgdown()` after the new export
- [ ] End to end against live 2012 frames: 186 UltraCam Xp at ~5,273 m and 21 UltraCam X
      at ~4,066 m, cross-checked against the DEM route
- [ ] PWF checkboxes match landed work
- [ ] `/planning-archive` on completion
