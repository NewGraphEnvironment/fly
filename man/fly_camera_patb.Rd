% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fly_camera_patb.R
\name{fly_camera_patb}
\alias{fly_camera_patb}
\title{Read the camera a frame's PAT-B file names}
\usage{
fly_camera_patb(
  photos_sf,
  dest_dir = tempfile("fly_patb"),
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{photos_sf}{An sf object with airphoto metadata, typically from the BC
Data Catalogue centroid layer. Must contain \code{patb_georef_url}, and
\code{film_roll} and \code{frame_number} or \code{airp_id} to join on.}

\item{dest_dir}{Directory to cache the downloaded archives in. Defaults to a
session temporary directory; give it a real path to keep them between runs.}

\item{overwrite}{If \code{FALSE} (default), archives already in \code{dest_dir} are
reused rather than re-downloaded.}

\item{quiet}{Suppress the per-archive progress messages.}
}
\value{
\code{photos_sf} with four columns added, all \code{NA} where nothing resolved:
\describe{
\item{\code{camera_serial}}{the camera or lens serial the file publishes}
\item{\code{camera_name}}{the model string the file publishes, where it has one}
\item{\code{patb_gsd}}{ground sample distance in \strong{centimetres}, matching the
catalogue's own units. The catalogue's \code{ground_sample_distance} is 0 for
whole years of digital imagery, and this is where the number is}
\item{\code{patb_source}}{the archive each row was resolved from}
}
}
\description{
Downloads and parses the per-frame georeferencing files the BC Data Catalogue
publishes through \code{patb_georef_url}, and attaches the camera identity they
carry to the photo centroids. \code{\link[=fly_footprint]{fly_footprint()}} then sizes those frames from
the named camera instead of refusing them, which needs no DEM.
}
\details{
This is the only function in the package that reaches the network on the
sizing path. \code{\link[=fly_footprint]{fly_footprint()}} takes the columns it produces and never
fetches anything itself, so a batch can be resolved once and reused.

Three file schemas are in circulation and they are dispatched on the columns
present rather than the file extension, because the extension does not
identify them. Two of the archives referenced by the frames this exists for
are 404s served as HTML under a \code{.csv} name; those frames are reported as
unresolved, by a message naming the archive rather than by an error, so one
missing file does not cost a batch its other frames.

A serial that the shipped camera table does not recognise is \strong{refused}
rather than resolved from the model string beside it. One published archive
labels both its cameras \verb{UltraCam XP} while one of them is an UltraCam X, a
20\% difference in ground width — so the model string is read only where the
file carries no serial at all. See \code{inst/notes/camera-formats.md}.
}
\examples{
centroids <- sf::st_read(system.file("testdata/photo_centroids_digital.gpkg",
                                     package = "fly"), quiet = TRUE)

# Copy the bundled archives into a scratch directory, so this runs offline and
# writes nothing into the installed package
cache <- file.path(tempdir(), "patb")
dir.create(cache, showWarnings = FALSE)
file.copy(list.files(system.file("testdata", "patb", package = "fly"),
                     full.names = TRUE), cache)

resolved <- fly_camera_patb(centroids, dest_dir = cache)
table(resolved$camera_serial, useNA = "ifany")

# `fly_footprint()` picks the columns up from here. These frames also carry a
# calibration report, which wins; drop it and the PAT-B serial sizes them.
resolved$camera_calibration_url <- NA_character_
fp <- fly_footprint(resolved)
unique(fp$width_source)

}
