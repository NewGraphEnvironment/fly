% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fly_filter.R
\name{fly_filter}
\alias{fly_filter}
\title{Filter photos by spatial relationship with an AOI}
\usage{
fly_filter(photos_sf, aoi_sf, method = c("footprint", "centroid"), buffer = 0)
}
\arguments{
\item{photos_sf}{An sf point object with a \code{scale} column.}

\item{aoi_sf}{An sf polygon defining the area of interest.}

\item{method}{One of \code{"footprint"} (default) or \code{"centroid"}.}

\item{buffer}{Buffer distance in metres added to the AOI before testing
intersection (default 0). Applied in BC Albers (EPSG:3005).}
}
\value{
A subset of \code{photos_sf} that intersects the AOI.
}
\description{
Subsets photos whose footprint or centroid intersects the area of interest.
The footprint method catches photos whose centroid falls outside the AOI
but whose ground coverage overlaps it.
}
\examples{
centroids <- sf::st_read(system.file("testdata/photo_centroids.gpkg", package = "fly"))
aoi <- sf::st_read(system.file("testdata/aoi.gpkg", package = "fly"))
# Footprint method finds more photos than centroid method
fp_result <- fly_filter(centroids, aoi, method = "footprint")
ct_result <- fly_filter(centroids, aoi, method = "centroid")
nrow(fp_result) >= nrow(ct_result)

}
